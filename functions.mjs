function mostrarIdade(nome, idade) {
    return `a idade de ${nome} e ${idade}`
}

function mostrarCidade(nome, cidade) {
    return `a idade de ${nome} e ${cidade}`
}

function mostrarHobby(nome, hobby) {
    return `a idade de ${nome} e ${hobby}`
}

export {
    mostrarCidade,
    mostrarHobby,
    mostrarIdade
}